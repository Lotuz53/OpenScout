name: routed
version: "1"
snapshot_id: stage-a-2026-09-14
dataset:
  dev: evaluation/dataset/questions.dev.jsonl
  holdout: evaluation/dataset/questions.holdout.jsonl
executor:
  type: fixture
  corpus: tests/intelligence/fixtures/github_objects.json
  strategy: routed
  top_k: 10
model:
  provider: recorded_fixture
  name: lexical-proxy-v1
routing:
  factual: hybrid_rerank
  temporal: hybrid_rerank
  comparative: split_hybrid
  aggregate: sql_plus_hybrid
  relational: graphrag
  fallback: hybrid
cost:
  measure_latency_ms: true
  measure_tokens: true
promotion:
  default: false
